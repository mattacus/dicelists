{
  "name": "dicelists",
  "version": "1.0.0",
  "description": "Dicelist password generator in Node.js",
  "main": "dicelist_password_generator.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mock-server": "json-server --watch api_mock/db.json"
  },
  "author": "mattacus",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.1.0",
    "json-server": "^0.14.0"
  }
}
